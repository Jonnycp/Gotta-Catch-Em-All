<?php 
function distEuclidea(){

}

function matrice($pokemon){
    $pokDist=[];
    for ($i=0; $i < count($pokemon); $i++) {
        array_push();
    }


function ordinamento(){
    do
    {
            $scambiato = false;
            for( $i = 0, $c = count( $vettore ) - 1; $i < $c; $i++ )
            {
                if( $vettore[$i] > $vettore[$i + 1] )
        {
                    $temp = $vettore[$i + 1];
                    $vettore[$i + 1] = $vettore[$i];
                    $vettore[$i] = $temp;
                    $scambiato = true;
        }
            }
    }
    while($scambiato);
return $vettore;
} 

function estrazione(){
    
}

}
?>